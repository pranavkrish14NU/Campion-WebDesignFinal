// Importing the Campground model from the 'campground.js' file

import Campground from './campground.js';
import Booking from './booking.js';

// Exporting an object containing the Campground model
export default{
    Campground,
    Booking
}